<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Native (pre-Compose) window theme — dark navy background matching the
         brand palette so there's no white flash before Compose content draws. -->
    <style name="Theme.MediQuery" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0F172A</item>
        <item name="android:statusBarColor">#0F172A</item>
        <item name="android:navigationBarColor">#0F172A</item>
        <item name="android:colorBackground">#0F172A</item>
    </style>
</resources>
