# Runtime-only deps for HF Spaces (CPU-only, no training/eval/voice tools).
# Torch CPU wheel is installed first via --extra-index-url in the Dockerfile.

# Core ML — CPU-only torch installed via Dockerfile extra index
torch>=2.1.0,<2.5.0
transformers>=4.40.0,<4.49
sentence-transformers>=3.0.0,<4.0.0
safetensors>=0.4.0
sentencepiece>=0.2.0

# RAG stack — pin to compatible 0.3.x series
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langgraph>=0.2.0,<0.5.0
qdrant-client>=1.7.0
rank-bm25>=0.2.2

# API
fastapi>=0.108.0
uvicorn>=0.25.0
python-multipart>=0.0.6
slowapi>=0.1.9

# Data / utilities
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
tqdm>=4.66.0
loguru>=0.7.0
psutil>=5.9.0
